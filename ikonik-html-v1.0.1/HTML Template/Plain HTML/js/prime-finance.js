document.addEventListener('DOMContentLoaded', function () {
  const btn = document.getElementById('connectWalletButton');
  if (btn) {
    btn.addEventListener('click', function () {
      alert('Функция подключения кошелька будет доступна позже.');
    });
  }
});
